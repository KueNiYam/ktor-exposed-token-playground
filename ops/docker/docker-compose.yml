services:
  tokenizer-api:
    build:
      context: ../..
      dockerfile: ops/docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
    restart: unless-stopped
    
  # 향후 확장용 (Redis, DB 등)
  # redis:
  #   image: redis:alpine
  #   ports:
  #     - "6379:6379"
